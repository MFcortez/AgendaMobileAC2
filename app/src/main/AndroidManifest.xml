<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agendamobileac2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_add"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetoAgenda">
        <activity android:name="com.example.agendamobileac2.ui.activities.FormularioPersonagemActivity"/>
        <activity android:name="com.example.agendamobileac2.ui.activities.ListaPersonagemActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>